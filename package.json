{
	"name": "desgin-system",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"author": "Marvin Mussacate",
	"scripts": {
		"format:write": "npx biome format --write",
		"format": "npm run format:write",
		"dev": "turbo run dev",
		"build": "turbo run build",
		"changeset": "changeset",
		"version-packages": "changeset version",
		"release": "turbo run build --filter=@urassamx7/docs... && changeset publish"
	},
	"devDependencies": {
		"@changesets/changelog-github": "^1.0.0",
		"@changesets/cli": "^3.0.1",
		"turbo": "^2.10.7"
	},
	"devEngines": {
		"packageManager": {
			"name": "npm",
			"version": "11.12.1"
		}
	}
}
